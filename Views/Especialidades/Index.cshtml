@model IEnumerable<AgendaMedica.Models.Especialidade>

@{
	ViewData["Title"] = "Especialidades";
}

<div class="container text-light mt-5">
	<h1 class="text-center display-5">Cadastro de <span class="fw-bold">Especialidades</span></h1>


	<p>
		<a asp-action="Create" class="btn btn-outline-light btn-lg w-25"><i class="bi bi-plus-circle"></i>Adicionar</a>
	</p>
	<table class="table table-info table-striped table-borderless">
		<thead>
			<tr>
				<th>
					@Html.DisplayNameFor(model => model.Nome)
				</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model)
			{
				<tr>
					<td>
						@Html.DisplayFor(modelItem => item.Nome)
					</td>
					<td>
						<a class="btn btn-info btn-sm" asp-action="Edit" asp-route-id="@item.EspecialidadeId">Editar</a> |
						<a class="btn btn-light btn-sm" asp-action="Delete" asp-route-id="@item.EspecialidadeId">Exclui</a>
					</td>
				</tr>
			}
		</tbody>
	</table>
